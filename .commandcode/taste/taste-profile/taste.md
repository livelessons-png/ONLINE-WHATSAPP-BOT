# Taste Profile
- Provides raw terminal/log output as evidence when reporting issues, rather than paraphrasing or summarizing. Confidence: 1.0
- Uses very terse, informal, abbreviated language when asking about errors (e.g., "why this resolve" or "hey fiix thiis n" instead of full sentences). Confidence: 0.9
- Expects explicit verification/confirmation that changes actually took effect — file edits persisted AND post-deployment state (e.g., "check please because i just deployed"), not just that they were attempted. Confidence: 0.9
- Does NOT want the assistant to burn time running diagnostic shell/PowerShell/curl/Python commands to debug environment issues on their machine — make the correct code changes and let the user test. BUT when the user explicitly enables the command feature and says to run checks ("run them now i have enabled cmmd"), they expect the assistant to actually execute the requested verification commands (e.g., `python -m py_compile`), not just hand back copy-paste instructions. Repeated fresh evidence: "correct all erros and test them now" — when handing over scripts, the user explicitly wants them tested, not merely delivered. Confidence: 0.8
- When the default shell/command execution path is broken (e.g., a failing foreground wrapper), the user expects the assistant to keep trying alternate execution channels — the dedicated PowerShell tool or background execution (`run_in_background` with `shell_tasks` status/exit-code checks) — to actually run verification, rather than declaring commands cannot be executed and handing back copy-paste instructions. Confidence: 0.7
- Once a working shell/PowerShell workaround is found (e.g., `run_in_background` bypassing a broken UAC `Start-Process -Verb RunAs` elevation wrapper), the user expects the assistant to retain and keep using that approach for the rest of the session — and to explain the root cause and mechanism clearly, including diagnostic commands for the user to fix the underlying environment issue. Confidence: 0.6
- Communicates feature requests as structured, numbered rule lists with bullet-point specifics (e.g., "1. SILENT DROP... 2. SILENT DROP...") rather than open-ended descriptions. Confidence: 0.8
- Provides complete, copy-paste-ready code blocks when requesting changes, rather than describing changes in prose. Confidence: 0.8
- Prefers production-ready, robust code with defensive edge-case handling (timeout retries, cold-start handling, comprehensive intent/keyword lists, input sanitization). Confidence: 0.7
- Prefers cloud-hosted/serverless architecture over local development — wants to eliminate local dependencies entirely and offload work to cheap/free cloud services (e.g., Render Free, Apps Script, MongoDB Atlas). Confidence: 0.8
- Prefers routing email notifications (escalations, alerts) through Google Apps Script rather than external email APIs (e.g., Brevo), because Apps Script has native access to the institution's Gmail/Workspace account, allowing emails to come directly from the institutional domain (@miva.university) without third-party API keys. Confidence: 0.7
- Embeds meta-instructions and inline reminders directly inside code snippets when requesting changes, rather than writing them as separate follow-up messages. Confidence: 0.6
- Uses external uptime monitoring services (e.g., cron-job.org pinging every 10 minutes) to keep Render Free tier services awake and prevent cold-start sleep. Confidence: 0.8
- Aggressively configures services to stay within free-tier resource limits — uses lightweight engines (e.g., NOWEB instead of Puppeteer), caps Node heap via NODE_OPTIONS, and directs ephemeral file storage to /tmp to avoid RAM/disk blowouts on 512MB instances. Confidence: 0.7
- Cares about proper Python code structure and indentation — notices and requests correction of misplaced imports, broken indentation, or syntax issues inside function bodies. Confidence: 0.8
- Diagnoses the root cause of bugs (e.g., mismatched tuple unpacking lengths from an API call) and provides the exact defensive fix pattern alongside the explanation, rather than simply describing the symptom and asking for a fix. Confidence: 0.6
- References files using `@` mentions (e.g., `@"filename.gs"`, `@filename.py`, `@WAHA_REMINDERV2.py`) to indicate which files the assistant should open and work on. Confidence: 0.8
- Wants precise, explicit confirmation of exactly which files were changed and which were left untouched after a code modification, rather than a summary of what was done. Confidence: 0.5
- Prefers **silent/graceful handling** of unsupported content — media, reactions, empty bodies, AND link-only messages (URL shares with no real query) are silently dropped rather than triggering any reply, including generic AI boilerplate (e.g., a Facebook link producing "I don't have access to external websites" was met with "wtf??????"). Expects link-only messages to be stripped of URLs and dropped before reaching the LLM, while text+link messages still get processed with the link ignored. Confidence: 0.8
- Requests comprehensive, cross-cutting refactoring — when a data model changes (e.g., single course → multi-course), expects ALL related code paths (data parsing, profile normalization, intercept handlers, system prompts) to be updated, not just the originating fetch function. Confidence: 0.7
- Prefers Apps Script (the data layer) to return raw structured JSON (e.g., `{"courses": ["SEN 301", "ECO 310"]}`) rather than pre-formatted text strings, so the consuming Python/LLM layer can interpret and format the data itself. Confidence: 0.8
- Expects sheet/row lookups to scan ALL matching rows and aggregate data (e.g., collect all course codes from every row matching a user), rather than stopping at the first match and returning a single value. Confidence: 0.8
- Prefers replacing hardcoded rule-based/if-else keyword-matching logic with an LLM-powered conversational brain for user interaction, to achieve fluid, natural conversation rather than robotic one-liners. Confidence: 0.8
- Designs system prompts with explicit, detailed persona definitions and strict behavioral guardrails (conversational intelligence rules, fact-guardrail constraints, graceful "warmly explain if data is missing" fallback patterns). Confidence: 0.8
- Favors a moderate temperature setting (e.g., 0.4) to balance "natural linguistic variety and conversational warmth" with low hallucination risk. Confidence: 0.7
- Prefers graceful, warm fallback behavior when data is unavailable — instructs systems to warmly explain that the information isn't accessible rather than guessing or fabricating facts. Confidence: 0.7
- Expects all previously working data sources and functionality to be preserved during architectural refactoring — silently dropping a data path (e.g., monthly lessons data) when replacing rule-based intercepts with an LLM is a defect, not an acceptable simplification. Confidence: 0.6
- Wants to understand the reasoning behind significant architectural changes — asks "why" when intercepts or logic are removed/refactored, expecting a detailed explanation of the rationale rather than just accepting the change. Fresh evidence: after a multi-edit session on `WAHA_INTERACT.py`, asked "what did you add in waha interact and why????" and received a per-edit recap of what changed and the reason for each, including what was explicitly NOT touched. Confidence: 0.8
- Pragmatic and context-aware about risk — distinguishes between different subsystems (interactive bot vs. batch daemon) and only applies safety measures (e.g., cold-contact filtering) where the actual threat exists, not universally. Prefers minimal, targeted solutions over blanket defenses. Confidence: 0.6
- Avoids redundant fallback mechanisms when the primary solution is already reliable — e.g., no need for a fuzzy name matcher as backup when LID-to-phone conversion works perfectly. Not a fan of over-engineering with unnecessary defensive layers. Confidence: 0.6
- Expects a comprehensive, organized end-of-session report summarizing every change made, structured by file with clear categories (tables/groups), when explicitly requesting "a report of what you have achieved." The report should be scoped to the current session/day's work only, not the entire cumulative project history. Confidence: 0.6
- Provides structured diagnostic troubleshooting as numbered checklists with specific time-window tolerances and edge-case explanations, rather than vaguely reporting symptoms — proactively tells the assistant what likely culprits are and what precise ranges/conditions to check. Confidence: 0.6
- Prefers performance-optimized logic with early-exit / fast-fail patterns — flips data-access order so the cheapest/lightest check runs first, and expensive operations (like reading a spreadsheet) only execute when actually needed. Explicitly designs "flipped logic" to minimize I/O and avoid timeouts. Confidence: 0.7
- Understands and explains multi-layer timeout topology — distinguishes between client-side timeout (e.g., Python `requests` timeout), edge-server timeout (e.g., Google's 504 Gateway Timeout), and server-execution timeout, and addresses each at its appropriate layer rather than conflating them. Confidence: 0.7
- Advocates pragmatic two-phase remediation: apply a temporary buffer fix (e.g., raise timeout from 30s to 60s) to stop immediate failures, while simultaneously deploying the permanent architectural fix (e.g., fast-exit logic in the server). Rejects doing only one or the other as incomplete. Confidence: 0.8
- Prefers extracting meeting/class links from calendar events using a prioritized multi-source fallback chain: native `getHangoutLink()` → description regex scraping → `getLocation()` → sheet/profile fallback. Rejects checking only the location field. Confidence: 0.7
- Cares about idempotent processing in automated polling/daemon systems — explicitly diagnoses duplicate-message bugs by analyzing the polling-window math (e.g., 30-min window ÷ 5-min poll interval = 6 potential sends), and expects deduplication via unique ID tracking in the database (MongoDB compound indexes, sent-reminder collections) rather than relying on state-less time windows or message-layer dedup. Expects exactly one message per event — receiving two WhatsApp reminders for the same lecture (e.g., 1:17pm and 1:24pm for a 1:30pm lecture) is reported as a bug ("isn't one enough?"). This has recurred in a later report (1:16pm and 1:23pm for the same 1:30pm lecture), each time with the raw pasted messages as evidence. Confidence: 0.9
- Anti-ban conscious — proactively wants human-like timing between automated WhatsApp/WAHA dispatches (random delays of 10-25 seconds between messages) to avoid platform flagging and bans, not just dedup of content. Fresh evidence: approved extending human-like delays to the failure/retry path too (5-12s backoff on send failures so a flaky WAHA isn't hammered by rapid consecutive retries) — the anti-ban timing concern applies to all dispatch paths, not just successful sends. Also wants anti-ban coverage to extend beyond timing to silent drops of noise (statuses/groups/link-only) so the bot doesn't generate automated replies that look spammy. Confidence: 0.9
- Prefers querying calendar events anchored to start-of-day (00:00) rather than the current time, with a wider lookahead window (e.g., 7 days), to prevent "schedule amnesia" where events later in the same day are missed due to query timing or timezone shifts. Confidence: 0.7
- Prefers to keep tooling/configuration/IDE-specific directories (e.g., `.commandcode/`) out of version control by explicitly adding them to `.gitignore` — and when they still show up on GitHub, expects the distinction between ignore-rules (which only stop new files) and untracking already-committed files (`git rm -r --cached` + commit + push) to be diagnosed and fixed, so the folder is actually gone from the remote, not just ignored going forward. Fresh evidence: explicitly requested again — "please add comandcode folder to gitingore" — even though it was already present, then followed up tersely "it stiill apears on github" when the folder persisted on the remote. Confidence: 0.9
- Wants the assistant to provide concrete, copy-paste-ready terminal commands (e.g., `git init`, `git push ...`) for version control operations, not just verbal explanations of what to do. Confidence: 0.6
- User's `@file.py` scripts frequently arrive corrupted from copy-paste (broken indentation, line-wrap artifacts, extra instructions mixed into the code); expects the assistant to recognize this pattern and rewrite the file cleanly from scratch rather than patching individual errors. Repeated fresh evidence: `mongo_ping.py` and then `list_dbs.py` each hit the same mangled-indentation corruption as `check_index.py`/`cleanup_dupes.py`. Confidence: 0.8
- Rejects assistant-introduced architecture/database names that were never agreed upon — pushes back forcefully ("am not using lrasa analytics at all... where is lrasa coming from????") and expects the assistant to remember prior architectural decisions (e.g., an agreed migration to MongoDB) and to be able to trace/cite where every referenced component name came from, rather than silently assuming one. Confidence: 0.7
- Asks short, terse confirming questions to verify their mental model of what a change will do — before a deployment (e.g., "so with this push it will start reading from mongo???") or after one (e.g., "will this bot stop reading people's statuses?") — expects a plain-language explanation of exactly what will and won't change as a result, including caveats. Fresh evidence: after the status/link/delay fixes, asked "so its fool proof antiban i guess" and the assistant's honest answer separated what was protected from real residual ban risks (per-number velocity, content, session health) with an offered upgrade. Confidence: 0.7
- Wants the WhatsApp bot strictly scoped to its intended function — it should process only 1:1 text messages and silently ignore everything else, particularly status updates (sender `status@broadcast`), group messages (`@g.us`), and the user's own outgoing messages (`fromMe`). Privacy-conscious: concerned about the bot reading people's statuses and expects code-level guards for that scope boundary, not just dedup fixes. Fresh evidence: user correctly diagnosed ("i bet you he didnt send any message only updated status") that a bot reply was triggered by a status update, not a message — statuses can be attributed by WAHA to the author's own number (or LID) rather than `status@broadcast`, so the guard must be multi-layered (sender ID, payload type/event/eventType, raw JSON markers like `isStatus`/`statusV3`/`statusShare`), not just an exact-string match on the sender. Confidence: 0.7
- Restricts WhatsApp automation strictly to known contacts and people who have already messaged the bot — sends only to engaged/known numbers, never cold or unknown numbers; expects the code to skip unengaged numbers (WAHA 400/404 rejections treated as skips) rather than attempting unsolicited outreach. Confidence: 0.7
- Prefers conservative reminder cadence over maximum effectiveness — proactively trimmed the scheduled reminder tiers from 3 to 2 (24h + 4h only, dropping the 10-minute last-call tier) to reduce per-number message volume and keep a cleaner anti-ban profile, accepting the loss of the final nudge. Anti-ban/safety concerns override feature completeness when they conflict. Confidence: 0.6
- When the assistant edits Google Apps Script locally (a file the user deploys manually outside the git/Render pipeline), the user prefers to apply and redeploy the changes themselves — "just tell me what you removed ... so i manually remove and update deployment" — and expects exact diff-level instructions: the precise code block(s) to delete/replace (with line references), the follow-up structural fix (e.g., converting `else if` to `if` after removing the first tier), and the deployment path (Deploy → Manage Deployments → Edit → New version → Deploy), rather than a prose summary of the change. Fresh evidence: when asked to "just update the app script so i can copy and paste", the user wants the COMPLETE updated section/block delivered verbatim for direct copy-paste into the Apps Script editor (not just removal instructions), with the assistant highlighting what changed vs. the old block so the user can verify; the user still handles the save/redeploy themselves. When the user says "update the @file" they mean edit the actual file on disk — "i said update the @\"inter+remind appscript.gs\"" — the on-disk file is the copy-paste source of truth, so the assistant must make the edit to the file itself (and verify/report its on-disk state) rather than only replying with chat instructions or pasted blocks. Confidence: 0.7
